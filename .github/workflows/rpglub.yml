name: Ubuntu - Rapidgator1

on:
  workflow_dispatch:

jobs:
  build:
    name: Start Building...
    runs-on: ubuntu-latest

    steps:
      - name: Downloading & Installing Essentials
        run: |
          wget -O rpgl.sh "https://raw.githubusercontent.com/Bogyi2024/log/main/dep_download/rpgl.sh"
          chmod +x rpgl.sh
          ./rpgl.sh
